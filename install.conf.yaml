- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: 
    ~/.gitconfig:
    ~/.config/starship.toml:
    ~/.config/nvim/init.vim:

- create:
    - ~/work

- shell:
  #  - [git submodule update --init --recursive, Installing submodules]
  - echo "hey folks"
  - sleep 2
  - 
    command: ./test.zsh
    stdout: true
    stderr: true
