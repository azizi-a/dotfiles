- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.config/starship.toml:
    ~/.config/nvim/init.vim:

- create:
    - ~/work

- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
